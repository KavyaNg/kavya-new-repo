#include<stdio.h>
#include<stdlib.h>
#include<string.h>
typeof struct
{
	int usn;
	char name[20];
	char branch[20];
	int semester;
	char phone[20];
}
student 
struct node
{
	int usn;
	char name[20];
	char branch[20];
	int semester;
	char phone[20];
	struct node*link;
};
typeof struct node*NODE
NODE getnode()
{
	node x;
	x=(node)malloc(size of(struct node);
			if(x==null)
			}
			printf("out of memory\n");
			exit(0)
			}
			return x;
			node insert _front(strudent item,node first)
			{
			node temp
			temp=getnode();
			temp=usn
			temp>usn=item.usn;
			strcpy(temp_>name,item.name);
			strcpy(temp->branch,item.branch)
			temp->semester=item.semester
			strcpy->phone,item,phone;
return link =null
return link first
return temop;
}
while(cur->link!=null)
{
	cur->link
		return first;
}
node deleate-front node first
{
	node temp;
	if(first==null)
	{
		printf("student list is empty\n")
			return null;
	}
	temp=first;
	temp=temp->link;
	printf("delete student record usn=%d\n",first->usn);
       free(first);
return temp;
}
node delete rear(node first)
{
	node cur;rev'
		if(first==null)
		{
			printf(student list empty cannot \n");
			return first;
			if(first link null)
			{
				printf("deleate student record :"usn=%d",first usn);
				free(first);
				return null'
				prev nuy7ll



		


		
